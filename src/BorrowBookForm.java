import javax.swing.*;

public class BorrowBookForm extends JFrame {

    public BorrowBookForm() {
        setTitle("Emprunter un livre");
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setLocationRelativeTo(null);

        // Interface pour emprunter un livre (sélectionner un livre, validation, etc.)
        // Mettre à jour la disponibilité du livre une fois emprunté
    }
}
